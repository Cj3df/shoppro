{
  "name": "shoppro-root",
  "version": "1.0.0",
  "description": "Root package for ShopMaster Pro",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
